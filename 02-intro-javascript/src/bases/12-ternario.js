// Operador condicional ternario

const activo = true;

// let mensaje = '';

// if (activo) {
//     mensaje = 'activo';
// } else {
//     mensaje = 'inactivo';
// }

//mensaje = activo ? 'activo': 'inactivo';
const mensaje = activo && 'Activo';

console.log(mensaje);